#!groovy
import hudson.EnvVars
import jenkins.model.*

node {
    
    stage("Get Artifacts") {

    }
    
    stage('Run test'){
        def testName = $params.test_name ?: 'test.robot' 
            dir("test") {
                sh "robot " + testName
            }
    }
}
